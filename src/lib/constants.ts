export const BLESSING_BY = "";

/**
 * Canonical site origin. Used in canonical/og:url/og:image absolute URLs,
 * sitemap, JSON-LD, and hreflang alternates. Single source of truth — change
 * here when the domain moves (e.g. to palomnik.md).
 */
export const SITE_ORIGIN = "https://path-with-care.lovable.app";
